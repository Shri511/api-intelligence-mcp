apiVersion: v1
kind: Service
metadata:
  name: api-intelligence-mcp
  labels:
    app: api-intelligence-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: api-intelligence-mcp
    component: mcp-server
